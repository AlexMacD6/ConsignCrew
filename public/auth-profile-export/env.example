# =============================================================================
# DATABASE
# =============================================================================
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://user:password@localhost:5432/yourdb"

# =============================================================================
# BETTER AUTH CONFIGURATION
# =============================================================================
# Secret key for Better Auth (generate a secure random string - 32+ characters)
# You can generate one with: openssl rand -base64 32
BETTER_AUTH_SECRET="your-secure-32-character-random-string-here"

# Base URL for your application
BETTER_AUTH_URL="http://localhost:3000"

# Public app URL (for client-side)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# =============================================================================
# EMAIL SERVICE (AWS SES)
# =============================================================================
# AWS credentials for sending emails (verification, password reset)
AWS_ACCESS_KEY_ID="your-aws-access-key-id"
AWS_SECRET_ACCESS_KEY="your-aws-secret-access-key"
AWS_REGION="us-east-1"

# Email address to send from (must be verified in AWS SES)
AWS_SES_FROM_EMAIL="noreply@yourdomain.com"

# =============================================================================
# OAUTH PROVIDERS (OPTIONAL)
# =============================================================================
# Google OAuth (currently disabled in code - uncomment to enable)
# GOOGLE_CLIENT_ID="your-google-client-id"
# GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Facebook OAuth (currently disabled in code - uncomment to enable)
# FACEBOOK_CLIENT_ID="your-facebook-app-id"
# FACEBOOK_CLIENT_SECRET="your-facebook-app-secret"

# =============================================================================
# PRODUCTION SETTINGS
# =============================================================================
# For production, update these values:
# - DATABASE_URL to your production database
# - BETTER_AUTH_URL to your production domain (https://yourdomain.com)
# - NEXT_PUBLIC_APP_URL to your production domain
# - AWS_SES_FROM_EMAIL to your verified production email
# - Generate a new BETTER_AUTH_SECRET (never reuse from development)

# =============================================================================
# NOTES
# =============================================================================
# 1. Never commit this file to version control (.env should be in .gitignore)
# 2. Keep .env.example (this file) in version control as a template
# 3. Generate strong, unique secrets for production
# 4. Verify your sender email in AWS SES before testing
# 5. For local development, you can use AWS SES sandbox mode

